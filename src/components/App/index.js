import React from "react";
import styles from "./index.module.scss"

const App = (props) => {
  return(
    <div className={ styles.App }>
      <h1> Hello, World! </h1>
    </div>
  );
}

export default App;